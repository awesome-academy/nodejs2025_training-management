doctype html
html
  head
    meta(charset='UTF-8')
    style.
      table {
        border-collapse: collapse;
        width: 100%;
        margin-top: 10px;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 8px;
      }
      th {
        background-color: #f2f2f2;
      }
  body(style='font-family: Arial, sans-serif; font-size: 14px; color: #333;')
    p Dear Supervisor,
    p
      | Please find below the progress summary of trainees for the course 
      strong "#{courseName}"
      | .

    table
      thead
        tr
          th #
          th Name
          th Email
          th Progress
          th Status
      tbody
        each trainee, index in trainees
          tr
            td #{index + 1}
            td #{trainee.name}
            td #{trainee.email}
            td #{trainee.progress}%
            td #{trainee.status}

    p(style='margin-top: 20px;') Thank you for your support.
    p 
      strong Training Management System
